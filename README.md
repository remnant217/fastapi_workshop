# 🚀 FastAPI Workshop — Асинхронный backend с авторизацией, логированием и Alembic

Учебный проект, демонстрирующий современные возможности разработки backend-приложений на **FastAPI**:

- асинхронная работа с БД через **SQLAlchemy 2.0 + aiosqlite**  
- миграции схемы БД через **Alembic**  
- **JWT-аутентификация** и разграничение прав (пользователь / суперпользователь)  
- логирование через **loguru** и middleware  
- модульная архитектура (routers / repositories / core / models)  
- **юнит-тесты** для слоя репозиториев на **pytest + pytest-asyncio**

---

## Функциональность

### Пользователи
- Регистрация нового пользователя (`POST /users/signup`)
- Создание пользователя админом (`POST /users/`)
- Получение текущего пользователя (`GET /users/me`)
- Получение пользователя по username с проверкой прав (`GET /users/{username}`)
- Список всех пользователей (только админ) (`GET /users/`)
- Частичное обновление пользователя (`PATCH /users/{user_id}`)
- Удаление пользователя (только админ, нельзя удалить себя) (`DELETE /users/{user_id}`)

### Авторизация
- Вход по логину и паролю и получение **JWT-токена доступа**  
  `POST /login/access-token` — принимает `username` и `password` (формат `application/x-www-form-urlencoded`, как в OAuth2) и возвращает `{"access_token": "...", "token_type": "bearer"}`.
- Защита эндпоинтов через:
  - `get_current_user` — проверка валидности JWT и активного статуса пользователя
  - `get_current_superuser` — проверка флага `is_superuser`

### Продукты
- Получение продукта по ID (`GET /products/{product_id}`)
- Создание продукта (`POST /products/`)
- Обновление продукта (`PUT /products/{product_id}`)

(В данном варианте все продуктовые эндпоинты публичные, без авторизации)

### Логирование и middleware
- Настройка логирования через **loguru**:
  - логи пишутся в `logs/app.log`
- HTTP-middleware:
  - измеряет время обработки каждого запроса
  - пишет лог вида: `METHOD PATH выполнялся XX.XX мс`
  - добавляет заголовок ответа `X-Process-Time-ms: <ms>`

---

## Технологический стек

Основные библиотеки (см. `requirements.txt`):

- **Python 3.12+**
- **FastAPI**
- **Starlette**
- **Pydantic v2**
- **SQLAlchemy 2.0 (async)**
- **aiosqlite**
- **Alembic**
- **PyJWT**
- **pwdlib**
- **loguru**
- **pytest**, **pytest-asyncio**

---

## Структура проекта

```
.
├── alembic/                    # Миграции БД
│   ├── env.py                  # Конфигурация Alembic, привязана к app.core.database
│   └── versions/               # Скрипты миграций
├── app/
│   ├── main.py                 # Создание FastAPI-приложения, подключение роутеров, логов, middleware
│   ├── api/
│   │   ├── deps.py             # Зависимости: AsyncSession, текущий пользователь, суперпользователь
│   │   └── routes/
│   │       ├── login.py        # Эндпоинт логина и выдачи JWT
│   │       ├── products.py     # CRUD для продуктов
│   │       └── users.py        # CRUD для пользователей, проверка прав
│   ├── core/
│   │   ├── database.py         # Async/Sync движки для SQLite, фабрика AsyncSessionLocal
│   │   ├── logging.py          # Конфиг loguru (файл logs/app.log)
│   │   ├── middleware.py       # Middleware для логирования времени выполнения
│   │   └── security.py         # JWT, SECRET_KEY, хэширование паролей
│   ├── models/
│   │   ├── __init__.py         # Base для ORM и импорты моделей
│   │   ├── products.py         # ORM-модель Product + Pydantic-схемы
│   │   ├── tokens.py           # Pydantic-схемы Token, TokenData
│   │   └── users.py            # ORM-модель User + Pydantic-схемы
│   ├── repositories/
│   │   ├── products.py         # Взаимодействие с БД для Product 
│   │   └── users.py            # Взаимодействие с БД для User
│   └── create_superuser.py     # Скрипт создания первого суперпользователя
├── logs/
│   └── app.log                 # Логи приложения
├── tests/
│   ├── conftest.py             # Асинхронные фикстуры: in-memory БД, сессия
│   ├── test_database.py        # Тестовый движок и сессии (SQLite in-memory)
│   ├── test_repository_products.py 
│   └── test_repository_users.py
├── alembic.ini                 # Общая конфигурация Alembic
└── requirements.txt            # Зависимости проекта
```

---

## Подготовка окружения

1. **Клонировать репозиторий**  
```bash
git clone <url>
cd <папка_проекта>
```

2. **Создать и активировать виртуальное окружение**  
```bash
python -m venv venv
# Windows:
venv\Scripts\Activate
# macOS / Linux:
source venv/bin/activate
```

3. **Установить зависимости**  
```bash
pip install -r requirements.txt
```

---

## Настройка базы данных

Проект использует SQLite-файл `products.db` в корне (рядом с `app/`). Путь к нему задан в `app/core/database.py`:

```python
DATABASE_ASYNC = 'sqlite+aiosqlite:///./products.db'
DATABASE_SYNC = 'sqlite:///./products.db'
```

### Применение миграций Alembic

```bash
alembic upgrade head
```

Если Alembic не видит модули `app.*`, нужно выполнить:

```bash
set PYTHONPATH=.
# или:
export PYTHONPATH=.
alembic upgrade head
```
## Создание суперпользователя

В проекте есть утилита `app/create_superuser.py`, которая создаёт первого администратора с логином и паролем по умолчанию:

```python
username = 'admin'
password = 'admin123'
```

### Запуск (из корня проекта):

```bash
python -m app.create_superuser
# или
python app/create_superuser.py
```

Скрипт:

- проверит, существует ли уже пользователь с username `admin`
- создаст его как `is_superuser=True`, `is_active=True`, если не существует
- выведет в консоль логин/пароль

---

## Запуск приложения

Из корня проекта:

```bash
uvicorn app.main:app --reload
```

По умолчанию приложение будет доступно по адресу:

- Swagger UI: http://127.0.0.1:8000/docs
- ReDoc: http://127.0.0.1:8000/redoc
---

## Как авторизоваться и использовать API

### Получить токен доступа

1. Откройте Swagger UI: `http://127.0.0.1:8000/docs`

2. Найдите эндпоинт `POST /login/access-token`

3. Нажмите **Try it out** и заполните поля формы:
   - `username`: ваш логин (например, `admin`)
   - `password`: ваш пароль (например, `admin123`)

4. Нажмите **Execute** — в ответе придёт JSON:

```json
{
  "access_token": "<JWT>",
  "token_type": "bearer"
}
```

---

### 3. Роли и доступ

**Обычный пользователь:**
- `GET /users/me` — информация о себе
- `GET /users/{username}` — только если `username` совпадает с его логином
- `PATCH /users/{user_id}` — только свой `user_id`

**Суперпользователь (`is_superuser=True`):**
- может вызывать всё перечисленное выше
- плюс:
  - `GET /users/` — список всех пользователей
  - `POST /users/` — создание пользователей с любыми флагами `is_active` и `is_superuser`
  - `PATCH /users/{user_id}` — обновление любого пользователя
  - `DELETE /users/{user_id}` — удаление любого пользователя, кроме самого себя

---

## Запуск тестов

Тесты покрывают слой репозиториев (продукты и пользователи) и работают с отдельной in-memory БД SQLite.

Убедитесь, что виртуальное окружение активно, и выполните:

```bash
pytest
# или:
python -m pytest
```

Используются:
- отдельный асинхронный движок  
  `TEST_DATABASE_ASYNC = 'sqlite+aiosqlite:///:memory:'`
- `StaticPool`, чтобы одна in-memory БД была доступна для всех тестов
- фикстура `session()` выдаёт новую `AsyncSession` для каждого теста

---

## Полезные эндпоинты

### Пользователи
- `POST /users/signup` — регистрация обычного пользователя
- `POST /login/access-token` — вход и получение JWT
- `GET /users/me` — профиль текущего пользователя
- `GET /users/{username}` — данные пользователя (с проверкой прав)
- `GET /users/` — список пользователей (только для админа)
- `POST /users/` — создание пользователя (только для админа)
- `PATCH /users/{user_id}` — обновление пользователя (с проверкой прав)
- `DELETE /users/{user_id}` — удаление пользователя (только для админа, нельзя удалить себя)

### Продукты
- `GET /products/{product_id}` — получить продукт
- `POST /products/` — создать продукт
- `PUT /products/{product_id}` — обновить продукт
